function getRandomNumberBetween(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}

// Example usage:
const num1 = 10;
const num2 = 15;
const randomNumber = getRandomNumberBetween(num1, num2);
console.log(`Random number between ${num1} and ${num2}: ${randomNumber}`);
